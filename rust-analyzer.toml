# rust-analyzer configuration
#
# The three facade features (tracing / log-backend / slog-backend) are mutually
# exclusive; enabling all of them triggers an E0252 duplicate-definition error in
# facade.rs, which cascades into misleading IDE errors. So pin static analysis to
# the default tracing facade here.
[cargo]
allFeatures = false
features = ["tracing"]
